puts "hello world"



puts "1 + 1 = 2"

# git setup
# git init to create a repo (rails does this for you)
# create a repo on gitHub
# add the remote link (remember to use SSH link on github)

# make changes in your code
# add(stage) changes to the working tree git add .
# commit those changes git commit -m'description of changes'

# when needed
# pull from github (if needed)
# fix any conflicts if they exist
# push up to gitHub git push origin main

# if working with a partner
# one person creates repo, the others will clone down repo(once)

# When a team member pushes to master, they should tell everyone to pull
# pull from master (i.e., git pull origin main)